# 墨流 Inkflow vNext｜创始人晨报

日期：2026-07-23
当前状态：本地成品、测试、证据和 Git 检查点全部完成；公开推送与正式站发布等待负责人最终确认。

## 一句话结果

墨流已经从“另一款专注计时器”重构为面向高频 AI Web / Vibe Coding 创作者的注意力连续性工具：在异步等待前封存回来后的第一步，在结果到达时用 Return Gate 一击接回。

## 首批用户与核心问题

- 首批用户：频繁在浏览器中调用 AI、等待生成、切换资料并继续编辑的个人创作者。
- 真问题：不是缺少倒计时，而是 AI 等待制造认知真空；回来后要重新寻找“刚才做到哪、下一步是什么”。
- 最小有效动作：`把回来后的第一步封存，再把它一击接回。`
- 北极星候选：Return Confirmed Rate，而不是累计专注分钟。

## 已交付的产品闭环

1. 选择 AI、工作、阅读或冥想场景。
2. 写下当前目标和“回来后的第一步”。
3. 封存 checkpoint，进入低刺激脑间歇。
4. 通过手动返回或诚实标注的模拟适配器收到返回信号。
5. Return Gate 只呈现此前封存的动作，一击接回。
6. 完成动作并做极短复盘；本地历史形成下一轮连续性证据。

Drift Rescue 是第二入口：它不声称自动检测走神，只在用户主动察觉漂移时帮助用一句话重新封存下一步。

## 工程与体验完成项

- 7 状态显式状态机：idle、intention、waiting、drift、return、active、review。
- `inkflow:vnext:*` 独立 local-first 数据命名空间，未修改旧版数据。
- 刷新恢复、跨标签 revision 同步、追加式事件记录。
- 手动/模拟来源标签，不伪装成尚未接入的第三方事件。
- 脑间歇、低能量模式、设置、历史、可选通知和 PWA 离线壳。
- 确定性 SSR 壳，修复真实回归中发现的 hydration mismatch。
- 石墨黑、骨白、信号橙视觉系统；以断开并重新接合的水平线表达注意力连续性。
- 旧版应用、开场组件和获批媒体全部保留，可从 Git 历史恢复。

## 真实验证证据

- `npm run lint`：通过，0 error / 0 warning。
- `npm test`：通过，生产构建成功，8/8 测试通过。
- 浏览器手动链路：首次引导 → 封存 → 等待 → 返回 → 接回 → 完成 → 复盘，完整跑通。
- 浏览器模拟链路：20 秒模拟任务在刷新后恢复 checkpoint，并自动进入 Return Gate。
- hydration 回归：曾真实发现 SSR/客户端不一致；引入确定性 `FlowRoot` 后复测无脚本错误，刷新恢复仍成立。
- 桌面证据图：`docs/evidence/return-gate-desktop.png`。

## 已知边界

- 当前版本不读取 Prompt、代码内容或第三方 AI 页面；核心动作和历史只存在本机浏览器。
- 第三方完成事件仍是手动/模拟适配器，这是来源诚实的产品选择，不是假装已接入。
- 当前测试浏览器没有可用的移动视口控制接口，因此未伪造窄屏截图；CSS 已覆盖 1050px 与 720px 断点，真实手机回归列为发布后验证项。
- 暂无云身份和跨设备同步；这避免第一版引入隐私与账户复杂度。

## Git 检查点与恢复

- `bb684f9`：v0.1 基线，可作为完整回滚点。
- `7fded0f`：A–I 产品方向与研究。
- `64e9ca2`：J–T 体验架构、技术架构与三个原型。
- `8deb7e5`：vNext 产品实现、测试、证据与交付文档。
- 当前工作分支：`codex/rebuild-inkflow-next`。

恢复旧版可从 `bb684f9` 创建分支；不需要删除 vNext，也不需要触碰用户未提交的 `design/`、`video/` 或 `docs/handoff/`。

## 启动与验收

```powershell
npm install
npm run dev
```

本地打开 `http://localhost:3000/`。完整验收：

```powershell
npm run lint
npm test
```

## 发布状态

尚未执行公开发布。下一步需负责人确认后：将 `codex/rebuild-inkflow-next` 快进合并到公开仓库 `main`，推送 GitHub，等待 Cloudflare 自动部署，再在正式网址完成一次真实封存/返回/接回回归。
