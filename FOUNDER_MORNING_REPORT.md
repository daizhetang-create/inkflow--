# 墨流 Inkflow vNext｜创始人晨报

日期：2026-07-23

## 1. 一夜之后，墨流变成了什么

AI 等待前封存下一步、结果到达后一击接回的注意力连续性工具。

## 2. 我们推翻了什么

- 推翻“墨流的上位定义是阅读专注计时器”：阅读只是可迁移场景，不是产品内核。
- 推翻“开场动画、声景、呼吸、统计加在一起就会产生价值”：这些只能是资产，不能替代核心动作。
- 推翻“专注分钟越多越成功”：第一成功单位改为一次 `return_confirmed` 和一次可执行的上下文恢复。
- 推翻“用户需要被阻止离开”：墨流允许离开，训练的是不带羞耻地回来。
- 推翻“增加 AI 聊天框就属于 AI 时代”：产品价值来自人机异步等待产生的新型认知真空。
- 推翻“移动端是桌面缩小版”：手机使用底部伴随栏、单列输入和更短的信息路径。

## 3. 我们从世界学到了什么

1. **航空/医疗交接单**：中断前只留下下一班真正需要知道的最小状态，不复制整个工作现场。
2. **分布式系统 checkpoint / resume**：保存可恢复点，而不是强迫进程永不暂停。
3. **网络重连握手**：返回时先确认共同状态，再继续传输；对应 Return Gate 的一击接回。
4. **导航偏航重算**：偏离不被判失败，只从当前位置重新给出最小下一步；对应 Drift Rescue。
5. **F1 进站窗口**：高压短窗口只呈现当前唯一动作；对应 Return 时隐藏统计和次级功能。

完整的 18 个领域、40 个机制与 20 张迁移卡见 `docs/research/`。

## 4. 我们选择了什么

- **第一用户**：高频使用 Codex、Claude Code、Cursor 或其他 Agent 的个人 Web/Vibe Coding 创作者。
- **核心问题**：几十秒到几分钟的 AI 等待制造认知真空；用户离开后损失的是任务上下文，而不只是时间。
- **核心机制**：Return Gate——等待前封存“回来后的第一步”，有结果时把它变成唯一主动作。
- **安全网**：Drift Rescue——只在用户主动承认偏航时缩小下一步，不假装监控。
- **第一场景**：发出一次 AI 任务后，在打开第二个信息流之前完成 checkpoint。
- **最小不可删除动作**：把回来后的第一步封存，再把它一击接回。

## 5. 产品如何工作

1. 用户选择 AI、工作、阅读或冥想去向。
2. 写下当前目标与“回来后的第一步”。
3. 选择手动信号，或明确标注的 20 秒/5 分钟模拟。
4. 封存 checkpoint，进入 Waiting。
5. 选择留白、远眺、伸展或一句判断，真实进入 Recovery；也可以什么都不做。
6. 任务完成、失败或用户提前回来时进入 Return；刷新和跨标签不会丢失状态。
7. Return Gate 只显示此前封存的一小步，用户点击“一击接回”。
8. 完成这一步，回答一次“上下文回来了吗”，然后墨流退场。

## 6. 最有辨识度的一刻

等待时，代表认知线程的水平线在中央断开；Return 到达时两端重新向中心握手。中央不是倒计时、树、奖励或 Dashboard，而是用户亲手封存的那一句动作。点击整个 Return Gate 后，线恢复连续，界面只要求完成这一小步。这一刻把“离开—被接住—回来”变成一次可感知的产品行为。

## 7. 真实完成情况

### 已实现

- 八状态运行时：idle、intention、waiting、recovery、drift、return、active、review。
- 手动/模拟 WaitAdapter；任务完成、失败、提前回来和无人返回分支。
- schema v1 local-first 快照、迁移、损坏降级、事件、历史与只清 vNext 数据。
- 页面刷新、BroadcastChannel + storage 降级、陈旧 revision 防覆盖。
- 页面可见性记录但不推断漂移；通知拒绝记忆、静默模式和不含工作正文的提醒。
- PWA 离线应用壳、reduced-motion、低能量模式、桌面与移动响应式。
- 三个先行交互原型与正式 vNext 主应用；旧版代码和开场媒体未删除。

### 验证

- `npm run lint`：0 error / 0 warning。
- `npm test`：生产构建成功，24/24 测试通过。
- 真实浏览器：手动闭环、20 秒模拟、5 分钟 Recovery 刷新、失败 Return、双标签同步全部跑通。
- 移动端：本机 Chromium 以 390×844 视口截图；发现标题溢出后修复并复测。
- Git：`7fded0f`（A–I）、`64e9ca2`（J–T）、`8deb7e5`（首轮实现）、`233e25f`（完成审计硬化）。

### 部署与限制

- 公开 GitHub main 和正式站尚未更新：公开发布属于任务书明确要求等待确认的操作。
- 当前没有真实 Codex/Claude/Cursor 事件接入；界面只显示手动或模拟，不欺骗。
- 没有云账号和跨设备同步；不上传 checkpoint、Prompt、代码或浏览历史。

## 8. 我应该先看哪里

1. 启动入口：`START_HERE.md`。
2. 当前产品：运行 `npm.cmd run dev`，打开 `http://localhost:3000/`。
3. 产品内核：`docs/product/K_kernel.md`。
4. PRD：`docs/product/P_PRD_vNext.md`。
5. 状态机/数据：`docs/product/M_state_machine.md`。
6. 技术架构：`docs/technical/S_architecture.md`。
7. 完整工作流证据：`docs/qa/W_workflow_validation.md`。
8. 真人实验：`docs/validation/X_experiment_plan.md`。
9. 桌面/移动截图：`docs/evidence/`。

```powershell
npm.cmd install
npm.cmd run dev
npm.cmd run lint
npm.cmd test
```

旧版恢复点是 `bb684f9`；当前工作分支是 `codex/rebuild-inkflow-next`。

## 9. 仍未解决的问题

- 真人是否愿意在真实 AI 等待前反复写一句 checkpoint，而不把它当成额外工作。
- 微行动是否真的替代高刺激内容，还是会变成另一种离开任务的仪式。
- Return 提示在真实工作日中是否温和，何时会令人烦。
- 阅读和冥想迁移是否自然，还是只有 AI Coding 的频率足以支撑产品。
- 真实第三方 adapter 的权限、事件可信度和隐私边界尚未验证。
- 需要真实触屏手机复核软键盘、safe-area 与通知权限。
- 正式站的 Service Worker 升级、缓存淘汰与线上 20 秒闭环尚待发布后回归。

## 10. 明天最值得做的三件事

1. **先做 8 人 / 7 天真人试验**：以 Return Confirmed、上下文恢复和自然复用决定内核是否成立。
2. **只验证一个真实 adapter**：优先浏览器扩展或可核验的 Agent 完成事件；不能证明来源就继续 manual。
3. **完成发布后设备回归**：正式域名跑通 desktop、390px、真实手机、离线和 Service Worker 更新，不增加新功能。
